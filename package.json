{
  "name": "octopus",
  "version": "1.0.0-alpha",
  "description": "UBC CLF8 DRUPAL THEME (aka octopus)",
  "license": "MIT",
  "scripts": {
    "build-css": "node-sass --output-style expanded  --source-map true --recursive --output css sass",
    "watch-css": "node-sass -w --output-style expanded  --source-map true --recursive --output css sass"
  },
  "dependencies": {
    "node-sass": "^4.7.2"
  }
}
